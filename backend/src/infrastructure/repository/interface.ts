/*export interface IRepository<T> {
    create(data : CreateDatas) : Promise<T>
    update(id: string ,data : UpdateDatas) : Promise<T> //updateData'yı verdiğinde eğer belli property verilmediğinde onları değiştiriyor mu değiştirmiyor mu öğren
    delete(id: string) : Promise<T>
    findById(id: string) : Promise<T>
    findAll() : Promise<T[]>
}*/